---
"vite-plugin-doctest": major
---

Update minimum version of all peer dependencies:

- Version of `vite` must be 8.0.0 or later
- Version of `vitest` must be 4.1.0 or later
- Version of `typescript` must be 5.0.0 or later
